<UserControl x:Class="UI.Control.Hosts"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.Control"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Style="{StaticResource ResourceKey=UserControlPageDefault}"  FontSize="10" >

    <Grid x:Name="MainGrid" Background="White" Width="460">
        <Grid.RowDefinitions>
            <RowDefinition Height="40*"/>
            <RowDefinition Height="200*"/>
            <RowDefinition Height="200*"/>
            <RowDefinition Height="200*"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="hostsPanel" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Center">
            
        </StackPanel>
        <Grid Name="UpGrid" HorizontalAlignment="Stretch" Height="auto" VerticalAlignment="Stretch" Width="auto" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>  
            <TextBox Name ="tbHostName" HorizontalAlignment="Stretch" Text="users:" VerticalAlignment="Stretch" FontSize="20" AcceptsReturn="True"/>
        </Grid>

    </Grid>
</UserControl>
