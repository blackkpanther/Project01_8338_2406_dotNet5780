<UserControl x:Class="UI.MiniControl.Host"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.MiniControl"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="400" Style="{StaticResource ResourceKey=pageDefault}" Height="100" Width="400" FontSize="10">

    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <DockPanel>
                <Label Content="privateName" HorizontalAlignment="Left" />
                <TextBlock Text="{Binding host1.PrivateName}" HorizontalAlignment="Right" />
            </DockPanel>
           
        </StackPanel>

    </Grid>
</UserControl>
