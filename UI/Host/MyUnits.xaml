<Window x:Class="UI.MyUnits"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="MyUnits"  WindowStartupLocation="{StaticResource WSL}"
        Style="{StaticResource ResourceKey=pageDefault}" Width="1500" >
    <Grid>
        <local:UserControl1 HorizontalAlignment="Left"  VerticalAlignment="Top" />
        <local:UserControl1 HorizontalAlignment="Right"  VerticalAlignment="Top" />
        <local:UserControl1 HorizontalAlignment="Center"  VerticalAlignment="Top" Height="710" Margin="520,10,465,0" Width="515" />

    </Grid>
</Window>
